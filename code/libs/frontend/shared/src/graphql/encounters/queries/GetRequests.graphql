query GetEncounterChangesQuery($id: ID!) {
  encounterChange(id: $id) {
    id
    accepted
    dates {
      id
      date
      availabilityHome
      availabilityAway
    }

    homeComment {
      id
      message
    }

    awayComment {
      id
      message
    }
  }
}
