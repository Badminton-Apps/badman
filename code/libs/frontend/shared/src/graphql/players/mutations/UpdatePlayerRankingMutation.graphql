mutation UpdatePlayer($playerId: String!, $rankingPlace: RankingPlaceInput!) {
  updatePlayerRanking(rankingPlace: $rankingPlace, playerId: $playerId) {
    id
    lastRanking {
      id
    }
    rankingPlaces {
      id
    }
  }
}
