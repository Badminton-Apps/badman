mutation RemoveBasePlayerForSubEvent(
  $teamId: ID!
  $playerId: ID!
  $subEventId: ID!
) {
  removeBasePlayerForSubEvent(
    teamId: $teamId
    playerId: $playerId
    subEventId: $subEventId
  ) {
    id
  }
}
