mutation AddBasePlayerForSubEvent(
  $teamId: ID!
  $playerId: ID!
  $subEventId: ID!
) {
  addBasePlayerForSubEvent(
    teamId: $teamId
    playerId: $playerId
    subEventId: $subEventId
  ) {
    id
  }
}
