query GetEncounterQuery($id: ID!) {
  competitionEncounter(id: $id) {
    id
    date
    originalDate
    home {
      id
      name
    }
    away {
      id
      name
    }
    encounterChange {
      id
      accepted
    }
    draw {
      id
      subeventId
    }
  }
}
