query GetCompYearsQuery($id: ID!) {
  club(id: $id) {
    id
    teams {
      id
      slug
      name
      clubId
      subEvents {
        id
        event {
          id
          startYear
        }
      }
    }
  }
}
