query GetPlayers($where: SequelizeJSON, $ranking: Date, $includeRanking: Boolean!, $includeClub: Boolean!) {
  players(where: $where) {
    id
    memberId
    firstName
    lastName
    phone
    email
    competitionPlayer
    clubs @include(if: $includeClub) {
      name
    }
    rankingPlaces(where: { rankingDate: $ranking }) @include(if: $includeRanking) {
      rankingDate
      single
      double
      mix
    }
  }
}
