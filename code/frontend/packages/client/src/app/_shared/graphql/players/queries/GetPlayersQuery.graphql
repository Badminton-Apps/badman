query GetPlayers($where: SequelizeJSON, $includeClub: Boolean!) {
  players(where: $where) {
    id
    memberId
    firstName
    lastName
    phone
    email
    competitionPlayer
    clubs @include(if: $includeClub) {
      name
    }
  }
}
