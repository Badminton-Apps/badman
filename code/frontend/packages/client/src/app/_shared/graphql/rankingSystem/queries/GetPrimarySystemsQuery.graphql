query GetPrimarySystemsQuery($where: SequelizeJSON) {
  systems(where: $where) {
    id
    primary
    amountOfLevels
    caluclationIntervalLastUpdate
    differenceForUpgrade
    differenceForDowngrade
  }
}
