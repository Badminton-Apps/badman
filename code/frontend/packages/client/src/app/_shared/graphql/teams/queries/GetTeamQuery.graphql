query GetTeamQuery($id: ID!) {
  team(id: $id) {
    id
    name
    teamNumber
    abbreviation
    type
    preferredTime
    preferredDay
    active
    captain {
      id
      firstName
      lastName
    }
    locations {
      id
      name
    }
    players {
      id
      slug
      firstName
      lastName
      competitionPlayer
      base
      gender
      lastRanking {
        id
        single
        double
        mix
      }
    }
  }
}
