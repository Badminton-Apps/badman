#import "./subEventsFragment.graphql"

query GetEvent($id: ID!) {
  eventCompetition(id: $id) {
    id
    name
    startYear
    allowEnlisting
    type
    subEvents(order: "eventType") {
      ...SubEventsDetails
    }
  }
}
