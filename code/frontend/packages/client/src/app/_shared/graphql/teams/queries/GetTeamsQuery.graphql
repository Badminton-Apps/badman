query GetTeamsQuery($clubId: ID!, $active: Boolean!) {
  teams(clubId: $clubId, where: { active: $active }) {
    id
    slug
    name
    abbreviation
    type
    teamNumber
    captainId
  }
}
