
query GetTeamInfo($clubId: ID!, $subEventWhere: SequelizeJSON) {
  club(id: $clubId) {
    teams(where: {active: true}) {
      id
      type
      teamNumber
      subEvents (where : $subEventWhere) {
        id
        maxLevel
        minBaseIndex
        maxBaseIndex
        event{
          type
        }
        meta {
          teamIndex
          players {
            playerId
          }
        }
      }
      players {
        id
        fullName
        gender
        lastRanking {
          single
          double
          mix
        }
      }
    }
  }
}
