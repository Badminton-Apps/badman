import { LevelToLetterPipe } from './level-to-letter.pipe';

describe('LevelToLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new LevelToLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
