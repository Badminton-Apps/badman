mutation AddEvent($event: EventInput!) {
  addEvent(event: $event) {
    id
    tournamentNumber
    firstDay
    dates
    type
    uniCode
    name
    subEvents {
      drawType
      eventType
      id
      name
    }
  }
}
