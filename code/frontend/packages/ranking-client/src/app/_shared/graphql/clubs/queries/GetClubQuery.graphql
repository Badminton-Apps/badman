query GetClubsQuery($id: ID!) {
  club(id: $id) {
    id
    name
    abbreviation
    clubId
    teams(order: "name") {
      id
      name
      abbreviation
      players {
        id
        firstName
        lastName
      }
    }

    players {
      id
      firstName
      lastName
    }
  }
}
