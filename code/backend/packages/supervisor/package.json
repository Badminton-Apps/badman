{
  "name": "@badvlasim/supervisor",
  "description": "Ranking simulation supervisor Badminton Vlaanderen",
  "version": "3.14.8",
  "private": true,
  "dependencies": {
    "@badvlasim/shared": "^3.14.8",
    "fast-csv": ""
  },
  "scripts": {
    "start": "node -r ts-node/register ./src/server.ts ",
    "start:inspect": "node --inspect=5861 -r ts-node/register ./src/server.ts",
    "start:watch": "nodemon",
    "start:watch:inspect": "nodemon --exec node --inspect=5861 -r ts-node/register ./src/server.ts",
    "test:local": "jest --colors --watchAll",
    "test": "jest --config ./jest.config.js --runInBand",
    "lint": "eslint --fix -c ../../.eslintrc.js src/**/*{.ts,.tsx}"
  },
  "browserslist": [
    ">0.2%",
    "not dead",
    "not ie <= 11",
    "not op_mini all"
  ],
  "peerDependencies": {
    "moment": "^2.24.0"
  }
}
