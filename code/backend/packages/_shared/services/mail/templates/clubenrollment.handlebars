<p>Beste competitieverantwoordelijke, <br/>
  <br/>
  Onderstaande ploegen werden ingeschreven voor het volgende competitieseizoen {{years}}. 
  Zolang je deze niet meer wijzigt, blijft de inschrijving exact zoals hieronder behouden. 
  Wil je toch nog uitschrijven? Contacteer dan: <a href="mailto:info@badmintonvlaanderen.be">info@badmintonvlaanderen.be</a>. <br/>
  <br/>
  Bedankt en tot volgend seizoen!
</p>

<h2>Teams</h2>
<tr>
  <td style="padding-top:12px" class="mceSpacing-24" valign="top">
    <div class="mceText" style="font-size:16px;width:100%">
      {{#each club.teams as |team|}}
        {{#each team.entries as |entry|}}
          <p><strong>Team:</strong> {{team.name}} ({{team.abbreviation}})<br/>
          <strong>Reeks:</strong> {{entry.competitionSubEvent.event.name}} - {{entry.competitionSubEvent.name}} <br/>
          <strong>Kapitein:</strong> {{team.captain.firstName}} {{team.captain.lastName}} ({{team.captain.email}}, {{team.captain.phone}})
          </p>
          <strong>Basisspelers: </strong>
          {{> players/list players=team.players clientUrl=../../clientUrl}}
        {{/each}}
      {{/each}}
    </div>
  </td>
</tr>

<h2>Locaties</h2>
<tr>
  <td style="padding-top:12px" class="mceSpacing-24" valign="top">
    <div class="mceText" style="font-size:16px;width:100%">
      {{#each locations as |location|}}
        <p><strong>{{location.name}}</strong><br/>
        {{#each location.availabilities as |availability|}}
          {{#if availability.days.length }}
            <p><strong>Speelmomenten:</strong><br/>
              <ul>
              {{#each availability.days as |day|}}
                <li>{{day.day}}, {{day.startTime}} - {{day.endTime}}: {{day.courts}} velden</li>
              {{/each}}
              </ul>
            </p>
          {{else}}
            <p><strong>Geen speelmomenten</strong><br/>
          {{/if}}
          {{#if availability.exceptions.length }}
            <p><strong>Uitzonderingen:</strong><br/>
              <ul>
                {{#each availability.exceptions as |exception|}}
                  <li>{{{ dateFormat "LL"  exception.start}}} tot {{{ dateFormat "LL"  exception.end}}}: {{exception.courts}} velden</li>
                {{/each}}
              </ul>
            </p>
          {{/if}}
        {{/each}}
      {{/each}}
    </div>
  </td>
</tr>


<h2>Comments</h2>
<tr>
  <td style="padding-top:12px" class="mceSpacing-24" valign="top">
    <div class="mceText" style="font-size:16px;width:100%">
      {{#each comments as |comment|}}
         <p><strong>Reeks:</strong> {{comment.competition.name}}<br/>
         <strong>Wie:</strong> {{comment.player.firstName}}, {{comment.player.lastName}}{{#if comment.player.memberId}} ({{comment.player.memberId}}){{/if}} <br/>
        <strong>Bericht:</strong><br/>{{comment.message}}</p>
      {{/each}}
    </div>
  </td>
</tr>
