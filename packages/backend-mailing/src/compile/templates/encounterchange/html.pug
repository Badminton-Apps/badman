extends ../../layouts/layout.pug

block title
  h1.text-center
    | Verplaatsingsaanvraag

block content
  p.text-center
    p
      | Beste #{ captain },
      br
      br
      - const actionKey = ["ENDORSE", "REJECT", "COUNTER"].includes(action) ? action.toLowerCase() : "propose";
      | !{ translate('all.competition.change-encounter.email.action.' + actionKey, { args: { actingTeamName, home: encounter.home.name, away: encounter.away.name } }) }
    p
      a(href=url)
        | Klik hier om het verzoek te bekijken

    br
    br
    | Met vriendelijke groet,
    br
    | Badman
