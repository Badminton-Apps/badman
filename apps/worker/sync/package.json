{
  "name": "worker-sync",
  "version": "0.0.0",
  "private": true,
  "type": "commonjs",
  "scripts": {
    "build": "nx build worker-sync",
    "test": "nx test worker-sync",
    "lint": "nx lint worker-sync",
    "dev": "nx serve worker-sync"
  }
}
